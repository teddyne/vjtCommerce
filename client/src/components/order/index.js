import React from 'react'
import { withContainer } from '../../layouts/container'

const Order = () => {
    return (
        <div>
            Order
        </div>
    )
}

export default withContainer(Order)